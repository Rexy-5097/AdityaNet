---
id: STD-22
title: Retraction fails the build
status: active
verification: INTG
---

# STD-22 — Retraction fails the build

A `RETRACTION` anywhere in a claim's provenance DAG fails the build
([ADR-0024](../adr/ADR-0024.md)). `CORRECTION` and `DEPRECATION` render a notice.
