# 04 — Leakage Proof: Conditions A–D

**Conclusion: all four conditions are CONFIRMED. Leakage is PROVEN.** The production thresholds were derived from test-split predictions (A — confirmed by code, by array fingerprint, by exact metric reproduction, and by the repository's own prior audit), they are what the production service loads (B — sole code path, no overrides), no validation-only replacement supersedes them in production (C — the replacement exists but nothing in `app/` references it), and the filesystem provenance trail coherently supports this sequence (D — rsync-preserved mtimes, same-second output pairing, in-repo audit logs).

---

## Condition A — Production thresholds originate from predictions made on the test split

**VERDICT: CONFIRMED** — by four independent lines of evidence, each read or computed this session.

1. **Generator code.** `scripts/optimize_operational_policy.py` is the sole writer of `artifacts/operator_thresholds.json` (`THRESHOLDS_OUT`, line 57; write at 295–296; repo-wide grep found no other writer). Its inputs are `artifacts/calibration/probs.npy`/`labels.npy` (lines 55–56, loaded at 166–167). Its docstring, line 6: *"Sweeps calibrated probability thresholds on the saved **test-set** probabilities."*
2. **Array origin in code.** `scripts/train_patchtst.py` writes those arrays at lines 327–329 (`np.save(os.path.join(CALIB_DIR, "probs.npy"), all_probs)`) from `trainer.evaluate_test(test_loader)` (line 315), where the test loader reads `artifacts/research/test.parquet` (line 66; dataset `split_name="test"`, line 242). Header docstring lines 17–19: "probs.npy ← test sigmoid probabilities / labels.npy ← test true labels."
3. **Array fingerprint (computed this session).** `probs.npy` N=1,806,313; `labels.npy` positives=419,150 (23.2047%). Exact match to the test split per `artifacts/evaluation_audit_report.json` → `dataset_consistency` (`test_parquet_total: 1806313`, `test_parquet_pos: 419150`, `saved_labels_total: 1806313`, `saved_labels_pos: 419150`, `passed: true`). The validation split (1,568,759 rows, 4.07% positive) cannot produce this fingerprint.
4. **Exact metric reproduction (computed this session).** Applying t=0.46 and t=0.88 to these arrays reproduces the deployed file's embedded `yellow_selection` and `red_selection` blocks to **all six recorded decimal places** (yellow: precision 0.425314, recall 0.613888, tss 0.363247, far 0.574686, f1 0.502491; red: 0.517326, 0.353255, 0.253664, 0.482674, 0.41983), and ROC-AUC recomputes to 0.748514 = the file's `roc_auc`. The deployed values are a deterministic function of the test arrays.

Corroboration: the repository's own audit `artifacts/operator_trust_audit.json` records `"threshold_dataset": "test"` and `"test_data_used_for_optimization": true`, citing the identical chain.

## Condition B — Those thresholds are the ones actually loaded by the production inference service

**VERDICT: CONFIRMED.**

- `app/services/ml/inference.py:86` — constructor default `thresholds_path = os.path.join("artifacts", "operator_thresholds.json")`; the file is loaded at lines 121–126 and every policy value consumed: `yellow_threshold` (127), `red_threshold` (128), the three uncertainty-suppression tiers (131–133), and both confidence cutoffs (136, 138).
- The only instantiation in the application is **no-arg**: `app/api/v1/endpoints/inference.py:38` — `_service = SuryaNetInferenceService()` — so defaults apply. Grep across `app/` and `scripts/` (this session) found no call passing a different `thresholds_path`.
- The endpoint is mounted into the live API: `app/api/v1/api.py:11` (`prefix="/predict"`) and `app/main.py:54`.
- The loaded values drive the operational outputs: alert tiering at lines 149–151, tiered uncertainty suppression at 156, RED confirmation logic comparing rolling means against `self.red_threshold` at 196–228.

*Caveat, noted for completeness:* `uvicorn.log`/`uvicorn_sprint35.log` contain no threshold-loading log line (grep this session). The service logs its thresholds at init (line 142) but is lazily instantiated on the first `/predict` call (endpoint lines 30–45), so absence in those captures indicates no prediction was served during logging, not an alternative configuration. Condition B concerns what the production service loads, which the code path fixes deterministically; this caveat does not weaken it.

## Condition C — No later validation-only replacement supersedes them in the production path

**VERDICT: CONFIRMED.**

- A validation-only replacement **exists**: `artifacts/operator_thresholds_validation_only.json` (yellow=0.14, red=0.95), generated by `scripts/refine_thresholds.py` (OUTPUT_PATH, line 61; test files explicitly excluded, lines 63–68), mtime 2026-06-15 20:31:36 — *newer* than the deployed file (19:54:38).
- It is referenced **nowhere in the application**: `grep -rn "validation_only" app/` → no matches (this session). All references live in `scripts/` and `scratch/` (verifiers, hashers, inventories).
- `app/services/ml/inference.py` was last modified at 20:56:41 — **after** the replacement existed — and its final state still defaults to the leaked file (line 86).
- The replacement was even validated end-to-end (`scripts/backtest_operator_policy.py` → `artifacts/operator_backtest.json`, `"thresholds_source": ".../operator_thresholds_validation_only.json"`), demonstrating deployment was feasible and simply not done.

## Condition D — Filesystem evidence supports this deployment sequence

**VERDICT: CONFIRMED** (within the limits of a repository with no version control, which condition D anticipates).

- **Timestamp fidelity established:** the repo has no git (`MIGRATION_REPORT.md:39`; `scientific_validation_report.md` §7 `"git_commit": "none"`). It was migrated 2026-07-01 via `rsync -a`, which "preserves … timestamps" (`MIGRATION_REPORT.md:45`). Every examined file shows birth 2026-07-01 11:09–11:12 with mtime 2026-06-15 — mtimes earlier than creation are only possible if preserved from the source.
- **Coherent single-day sequence** (full table in `02_threshold_provenance.md`): test arrays 13:19:16 → calibrator 19:25:16 → policy script 19:38:23 → **thresholds JSON + sweep CSV both 19:54:38** (same second — exactly the two outputs the script emits in one run, lines 259 and 295) → readiness report 20:00:33 → corrective script 20:11:32 → validation-only artifact 20:31:36 → `inference.py` final edit 20:56:41.
- **Import/consumption chains** (Condition D's "script import chains"): `optimize_operational_policy.py` names its inputs and output in code; `inference.py` names its input in code; the endpoint instantiates with defaults. No dynamic indirection, environment override, or config file intervenes at any hop (verified by grep this session).
- **Audit logs** (Condition D's "audit logs"): `artifacts/operator_trust_audit.json` independently documents the leak (`"test_data_used_for_optimization": true`) with the same file/line citations; `artifacts/operator_backtest.json` documents that the corrective policy was tested and what it scored; `scratch/run_architecture_ceiling_audit.py:20` designates `operator_thresholds.json` as `OPERATIONAL_PROD_PATH`.
- **Content self-consistency:** the deployed JSON's embedded selection metrics are a byte-exact function of the test arrays (reproduced this session), binding the artifact's content to its claimed inputs independent of any timestamp.

**Residual unknowns, stated for honesty (none affect the verdict):** (i) what the 20:56:41 edit to `inference.py` changed cannot be recovered without version history — only its final state is provable; (ii) the generator of `artifacts/operator_readiness_report.json` is absent from the tree — that report's *procedure* is NOT PROVEN, though its dependence on the leaked thresholds is evident from its content and timing; (iii) no runtime log line captures the service loading the file (lazy init; see Condition B caveat).

---

## Summary

| Condition | Verdict | Decisive evidence |
|-----------|---------|-------------------|
| A — thresholds from test-split predictions | **CONFIRMED** | `optimize_operational_policy.py:6,55–56` + `train_patchtst.py:315,327–329,66` + array fingerprint (N=1,806,313 / pos=419,150 = test) + exact 6-dp metric reproduction |
| B — loaded by production service | **CONFIRMED** | `inference.py:86,121–142` + no-arg instantiation `endpoints/inference.py:38` + router mounting `api.py:11`, `main.py:54` |
| C — no supersession in production | **CONFIRMED** | `grep "validation_only" app/` → none; `inference.py` final mtime 20:56:41 post-dates replacement (20:31:36) and still points at leaked file |
| D — filesystem provenance coherent | **CONFIRMED** | rsync-preserved mtimes (`MIGRATION_REPORT.md:45`), same-second output pairing at 19:54:38, in-repo audit `operator_trust_audit.json`, content-to-input binding via exact reproduction |

**All four conditions hold simultaneously → LEAKAGE PROVEN.**
